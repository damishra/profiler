#! /usr/bin/bash

bandwidth_hog_pid=$(pidof bandwidth_hog)
bandwidth_hog_burst_pid=$(pidof bandwidth_hog_burst)
cpu_hog_pid=$(pidof cpu_hog)
disk_hog_pid=$(pidof disk_hog)
memory_hog_pid=$(pidof memory_hog)
memory_hog_leak_pid=$(pidof memory_hog_leak)

while true; do

done
